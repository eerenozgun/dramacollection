// Firestore Security Rules - Debug Mode (Geçici)
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Geçici olarak tüm erişime izin ver - DEBUG MODE
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
